# Khawam Printing - Production environment
# Run from repository root: docker compose -f deploy/docker-compose.production.yml up -d
# Optimized for 4GB RAM VPS

services:
  postgres-production:
    image: postgres:16-alpine
    container_name: khawam-postgres-production
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-khawam_production}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in production/.env}
      POSTGRES_DB: ${POSTGRES_DB:-khawam_production}
    volumes:
      - postgres_production_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-khawam_production} -d ${POSTGRES_DB:-khawam_production}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - khawam-production

  pgbouncer-production:
    image: edoburu/pgbouncer:1.22.1-p0
    container_name: khawam-pgbouncer-production
    restart: always
    depends_on:
      postgres-production:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-khawam_production}:${POSTGRES_PASSWORD}@postgres-production:5432/${POSTGRES_DB:-khawam_production}
      POOL_MODE: transaction
      MAX_CLIENT_CONN: 100
      DEFAULT_POOL_SIZE: 10
      AUTH_TYPE: scram-sha-256
    networks:
      - khawam-production

  backend-production:
    build:
      context: .
      dockerfile: deploy/docker/backend.Dockerfile
    image: khawam/backend-production:latest
    container_name: khawam-backend-production
    restart: always
    ports:
      - "127.0.0.1:4081:8000"
    depends_on:
      pgbouncer-production:
        condition: service_started
      postgres-production:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 8000
      DATABASE_URL: postgresql://${POSTGRES_USER:-khawam_production}:${POSTGRES_PASSWORD}@pgbouncer-production:5432/${POSTGRES_DB:-khawam_production}
      DIRECT_DATABASE_URL: postgresql://${POSTGRES_USER:-khawam_production}:${POSTGRES_PASSWORD}@postgres-production:5432/${POSTGRES_DB:-khawam_production}
      PGBOUNCER_ENABLED: "true"
      DB_CONNECTION_LIMIT: "10"
      QUEUE_POOL_MAX: "3"
      SECRET_KEY: ${SECRET_KEY}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-https://example.com}
      FRONTEND_URL: ${FRONTEND_URL:-https://example.com}
    networks:
      - khawam-production

  frontend-production:
    build:
      context: .
      dockerfile: deploy/docker/frontend.Dockerfile
      args:
        VITE_API_URL: /api
    image: khawam/frontend-production:latest
    container_name: khawam-frontend-production
    restart: always
    ports:
      - "127.0.0.1:4080:80"
    networks:
      - khawam-production

networks:
  khawam-production:
    driver: bridge

volumes:
  postgres_production_data:
